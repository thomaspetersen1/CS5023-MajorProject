# Override the rosbridge URL the page connects to. Defaults to ws://localhost:9090.
# For demos on lab wifi, set to ws://<lab-desktop-hostname>:9090 if the page
# is served from a different machine than the one running rosbridge.
NEXT_PUBLIC_ROSBRIDGE_URL=ws://localhost:9090
